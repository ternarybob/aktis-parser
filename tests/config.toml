# -----------------------------------------------------------------------
# Test Configuration for Aktis Parser
# -----------------------------------------------------------------------

[test]
parser_url = "http://localhost:8085"
timeout_seconds = 5

[api]
enabled = true
retry_count = 3
retry_delay_ms = 1000

[ui]
enabled = false
browser = "chromium"
headless = false
screenshot_on_failure = true
